{
  "name": "sehat-backend",
  "version": "0.1.0",
  "description": "FastAPI backend for Sehat",
  "main": "app/main.py",
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest"
  },
  "dependencies": {
    "fastapi": "^0.104.1",
    "uvicorn[standard]": "^0.24.0",
    "python-multipart": "^0.0.6",
    "supabase-py": "^1.37.0",
    "openai": "^1.3.0",
    "PyPDF2": "^3.0.1",
    "python-dotenv": "^1.0.0"
  },
  "devDependencies": {
    "pytest": "^7.4.0",
    "httpx": "^0.25.0"
  },
  "python": ">=3.8"
}